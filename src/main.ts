/**
 * Future ESM application entry point.
 *
 * This file is intentionally not wired into the production HTML yet.
 * It establishes a typed module entry that can absorb AppBase/Core and
 * FitTimer product modules incrementally without changing the legacy runtime.
 */
export const APPBASE_ESM_FOUNDATION = true;
