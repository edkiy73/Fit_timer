/* Runtime-настройки веб-версии. Мобильная сборка заменяет значения в dist/.
   Секретов здесь быть не должно: файл попадает в приложение и виден пользователю. */
window.FIT_TIMER_CONFIG = Object.freeze({
  apiBase: '',
  publicAppUrl: ''
});
